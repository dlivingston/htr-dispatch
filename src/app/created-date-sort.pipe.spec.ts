import { CreatedDateSortPipe } from './created-date-sort.pipe';

describe('CreatedDateSortPipe', () => {
  it('create an instance', () => {
    const pipe = new CreatedDateSortPipe();
    expect(pipe).toBeTruthy();
  });
});
